lineLibraryBtn').addEventListener('click', () => showPanel('#onlineLibraryPanel', false));
+$('#refreshOnlineLibraryBtn').addEventListener('click', refreshOnlineLibrary);
+$('#onlineLibrarySearch').addEventListener('input', () => { onlineLibraryPage = 1; renderOnlineLibrary(); });
+$('#onlineLibraryPrevBtn').addEventListener('click', () => { onlineLibraryPage = Math.max(1, onlineLibraryPage - 1); renderOnlineLibrary(); });
+$('#onlineLibraryNextBtn').addEventListener('click', () => { onlineLibraryPage += 1; renderOnlineLibrary(); });
 $('#closeNewsBtn').addEventListener('click', () => showPanel('#newsPanel', false));
 $('#newsScanBtn').addEventListener('click', runUpdateScan);
 $('#updateScanBtn').addEventListener('click', runUpdateScan);
@@ -1670,6 +1884,7 @@
 $('#downloadAllBtn').addEventListener('click', () => downloadCatalogChapters((selectedCatalog?.series?.chapters || []).map((chapter) => String(chapter.id))));
 $('#favoriteSelectedBtn').addEventListener('click', async () => { try { await toggleSelectedReadingList('favorite'); } catch (error) { setStatus(`Favorit konnte nicht geändert werden: ${error.message}`); } });
 $('#readingSelectedBtn').addEventListener('click', async () => { try { await toggleSelectedReadingList('reading'); } catch (error) { setStatus(`Leseliste konnte nicht geändert werden: ${error.message}`); } });
+$('#onlineLibrarySelectedBtn').addEventListener('click', async () => { try { await toggleSelectedOnlineLibrary(); } catch (error) { setStatus(`Online-Bibliothek konnte nicht geändert werden: ${error.message}`); } });
 $('#watchSelectedBtn').addEventListener('click', async () => {
   if (!selectedCatalog?.series) return;
   try {
@@ -2074,4 +2289,4 @@
   if (event.type === 'check-error') { setStatus(`Prüffehler: ${event.message}`); await render(); }
 });
 
-Promise.all([loadAppVersion(), loadSettings(), renderConnectors(), refreshWebsites(), refreshDownloads(), refreshQueue(), refreshPhoneSync(), refreshReadingList(), refreshNewsButton()]).then(async () => { renderCatalogChapters(); updateSiteDownloadControls(); await render(); }).catch((error) => setStatus(`Startfehler: ${error.message}`));
+Promise.all([loadAppVersion(), loadSettings(), renderConnectors(), refreshWebsites(), refreshDownloads(), refreshQueue(), refreshPhoneSync(), refreshReadingList(), refreshOnlineLibrary(), refreshNewsButton()]).then(async () => { renderCatalogChapters(); updateSiteDownloadControls(); await render(); }).catch((error) => setStatus(`Startfehler: ${error.message}`));
diff -ruN v113/src/renderer/index.html v123/src/renderer/index.html
--- v113/src/renderer/index.html	2026-08-30 15:32:56.607769876 +0000
+++ v123/src/renderer/index.html	2026-08-30 15:32:56.609589264 +0000
@@ -3,19 +3,20 @@
 <head>
   <meta charset="UTF-8" />
   <meta name="viewport" content="width=device-width, initial-scale=1.0" />
-  <title>Manhwa Watcher v1.1.2</title>
+  <title>Manhwa Watcher v1.2.0</title>
   <link rel="stylesheet" href="styles.css" />
 </head>
 <body>
   <header>
     <div>
-      <div class="eyebrow" id="versionLabel">v1.1.2 · Stable</div>
+      <div class="eyebrow" id="versionLabel">v1.2.1 · Online Library Fix</div>
       <h1>Manhwa Watcher</h1>
       <p>Quelle auswählen, einzelne Serien laden oder einen kompletten Katalog als CBZ-Archiv herunterladen.</p>
     </div>
     <div class="header-actions">
       <button id="updateScanBtn" class="accent">Updates scannen</button>
       <button id="newsToggleBtn" class="secondary">Neuheiten</button>
+      <button id="onlineLibraryToggleBtn" class="secondary">Online-Bibliothek</button>
       <button id="phoneSyncToggleBtn" class="secondary">Handy-Sync</button>
       <button id="websitesToggleBtn" class="secondary">Webseiten</button>
       <button id="labToggleBtn" class="secondary">Connector-Labor</button>
@@ -131,6 +132,7 @@
             <div class="selected-series-actions">
               <button id="favoriteSelectedBtn" class="secondary" disabled>☆ Favorit</button>
               <button id="readingSelectedBtn" class="secondary" disabled>📖 Leseliste</button>
+              <button id="onlineLibrarySelectedBtn" class="secondary" disabled>☁ Merken</button>
               <button id="watchSelectedBtn" class="secondary" disabled>Beobachten</button>
               <button id="syncSelectedBtn" class="secondary" disabled>📱 Sync</button>
               <button id="labSelectedBtn" class="secondary" disabled>Connector</button>
@@ -337,6 +339,30 @@
       <div id="newsList" class="news-list"><div class="empty compact">Nach dem nächsten Update-Scan erscheinen neue Kapitel hier.</div></div>
     </section>
 
+    <section id="onlineLibraryPanel" class="debug-panel hidden">
+      <div class="panel-head compact">
+        <div>
+          <div class="eyebrow">Online Library</div>
+          <h2>Titel speichern, ohne Kapitel herunterzuladen</h2>
+          <div id="onlineLibrarySummary" class="subtle">Nur Metadaten und Links werden gespeichert. Kapitel werden erst beim Öffnen live von der Quelle geladen.</div>
+        </div>
+        <div class="engine-actions">
+          <button id="refreshOnlineLibraryBtn" class="secondary">Aktualisieren</button>
+          <button id="closeOnlineLibraryBtn" class="secondary">Schließen</button>
+        </div>
+      </div>
+      <div class="online-library-toolbar">
+        <input id="onlineLibrarySearch" type="search" autocomplete="off" placeholder="Gespeicherte Titel durchsuchen …" />
+        <span id="onlineLibraryCount" class="pill">0 Titel</span>
+      </div>
+      <div id="onlineLibraryList" class="online-library-list"><div class="empty compact">Noch keine Titel gespeichert. Öffne eine Serie und klicke auf „☁ Merken“.</div></div>
+      <div class="online-library-pager">
+        <button id="onlineLibraryPrevBtn" class="secondary">‹</button>
+        <span>Seite <strong id="onlineLibraryPage">1</strong> / <strong id="onlineLibraryPages">1</strong></span>
+        <button id="onlineLibraryNextBtn" class="secondary">›</button>
+      </div>
+    </section>
+
     <section id="downloadsPanel" class="debug-panel hidden">
       <div class="panel-head compact">
         <div><div class="eyebrow">Downloadverlauf</div><h2>Heruntergeladene Kapitel</h2><div class="subtle">Wird dauerhaft gespeichert – auch für Serien, die nicht beobachtet werden.</div></div>
diff -ruN v113/src/renderer/styles.css v123/src/renderer/styles.css
--- v113/src/renderer/styles.css	2026-08-30 15:32:56.608011951 +0000
+++ v123/src/renderer/styles.css	2026-08-30 15:32:56.609724723 +0000
@@ -369,3 +369,60 @@
 .news-badge { padding:4px 8px; border-radius:999px; background:#173e30; color:#8ee7b6; font-size:10px; font-weight:800; white-space:nowrap; }
 .news-row button { padding:7px 9px; font-size:10px; }
 @media (max-width:900px) { .news-row { grid-template-columns:1fr auto; } .news-copy { grid-column:1 / -1; } }
+
+
+/* v1.2.0 – Online Library */
+.online-library-toolbar { display:flex; align-items:center; gap:10px; margin-bottom:10px; }
+.online-library-toolbar input { flex:1; min-width:180px; }
+.online-library-toolbar .pill { margin-left:0; }
+.online-library-list { display:grid; gap:7px; }
+.online-library-row { display:grid; grid-template-columns:auto minmax(0,1fr) auto auto auto; gap:9px; align-items:center; padding:9px 10px; border:1px solid #263452; border-radius:10px; background:#0b1224; }
+.online-library-cover { width:42px; height:58px; object-fit:cover; border-radius:6px; background:#151d31; }
+.online-library-copy { min-width:0; }
+.online-library-copy strong { display:block; color:#e7ebfb; font-size:12px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
+.online-library-copy small { display:block; margin-top:3px; color:#7f8caf; font-size:9px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
+.online-library-copy small.online-library-last { color:#8ed1ab; }
+.online-library-row button { padding:7px 9px; font-size:10px; white-space:nowrap; }
+.online-library-pager { display:flex; justify-content:center; align-items:center; gap:10px; margin-top:10px; color:#9aa6c8; font-size:11px; }
+.online-library-pager button { min-width:36px; padding:7px 10px; }
+button.online-read { background:#283b5a; color:#bcd7ff; }
+@media (max-width:900px) { .online-library-row { grid-template-columns:auto minmax(0,1fr) auto; } .online-library-row button { grid-row:auto; } }
+
+
+/* v1.2.2 – Online-Lesestatus pro Kapitel */
+.catalog-chapter-row.online-read-done { background:rgba(55,135,91,.065); box-shadow:inset 3px 0 rgba(83,191,126,.52); }
+.catalog-chapter-row.online-read-done:hover { background:rgba(55,135,91,.105); }
+.online-read-badge { display:inline-flex; align-items:center; margin-left:5px; padding:1px 5px; border-radius:999px; border:1px solid rgba(91,215,139,.36); background:rgba(65,190,112,.14); color:#8ee7b6; font-size:8px; font-weight:800; vertical-align:1px; }
+.catalog-chapter-copy small.read-done { color:#82d8a5; }
+button.online-read-status { backg